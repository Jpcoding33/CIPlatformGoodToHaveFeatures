@model MissionThemeDetails;

@{
    ViewData["Title"] = "AddMissionThemeDetail";
    Layout = "~/Views/Shared/_LayoutForAdmin.cshtml";
}


<div class="container-fluid p-0">
    <div class="border-bottom add-edit-heading">
        <h5 class="heading">Add Theme</h5>
    </div>
    <div class="form">
        <form asp-controller="Admin" asp-action="AddTheme" method="post">
            <div class="form-content">
                <div class="form-group">
                    <label class="form-label" for="title">Title</label>
                    <input asp-for="Title" type="text" class="form-control" id="title"
                           placeholder="Enter title">
                    <span class="text-danger" asp-validation-for="Title"></span>
                </div>
                <div class="form-group">
                    <label class="form-label" for="status">Select Status</label>
                    <select asp-for="Status" class="form-select select" id="status">
                        <option value="1">Active</option>
                        <option value="0">In-Active</option>
                    </select>
                    <span asp-validation-for="Status" class="text-danger"></span>
                </div>
            </div>
            <div class="d-flex justify-content-end align-items-center mt-3">
                <a asp-controller="Admin" asp-action="MissionThemeDetails" class="btn px-4 mb-5 cancel-btn rounded-pill me-4">Cancel</a>
                <button class="btn px-4 mb-5 submit-btn rounded-pill" type="submit">Save</button>
            </div>
        </form>
    </div>
</div>
<script type="text/javascript">
    $('#missTheme').addClass('active');
</script>